# First-person renderer replacement

Latest approved request adds crisp original procedural pixel textures: generate a small atlas in code with NEAREST filtering, grass top distinct from side, dirt, stone, bark, leaves, wool. Render actual textured cubes, not only solid colors. Small stepped terrain uses shared game.heightAt(mapX,mapY) -> ground top height (0, .25, .5, .75); called at tile centers; geometry and camera MUST use it, camera eye=heightAt(player.x,player.y)+1.6. Parent implements this pure method; fallback 0 only for isolated view tests. Map edges are bounded. Solid resources must have matching .4 radius footprint. Head/foliage cubes may occlude but must not become separate attack targets. Parent adds player.pitch default -0.22 and look(dx,dy). Parent will provide center pick; no need overlay minimap (maximize world space). Keep any minimap small if used. You own tests/lettercraft-view.test.cjs too: replace iso tests, test perspective axes/pitch, ray intersection + occlusion using real methods. Follow TDD for pure projection and ray functions. Parent independently owns core/bridge/UI/browser tests. Do not touch those. Use apply_patch for all source edits.

User now explicitly requests first-person view. Implement ONLY src/lettercraft/view.js, replacing isometric renderer. Do not edit other files, commit, or spawn agents. Parent owns movement/input/UI/tests/integration. Build a real perspective 3D voxel scene viewed from player eyes, using native WebGL (no new dependencies or remote library). Canvas overlay `#lc-labels` (2D context) exists alongside WebGL `#lc-canvas` for upright readable letter labels, first-person hand/tool, crosshair, minimap, particles, progress ring. Optional overlay argument in constructor(canvas, overlayCanvas); parent passes both. Keep same class name LettercraftView. No event listeners in renderer; bridge handles context loss if needed. Implement graceful clear error if WebGL unavailable.

Visual: Minecraft-inspired original block trees, boulders/ore, sheep and hostile green monsters, grass/dirt tiled ground 32x32, tan intersecting paths, sky/fog, directional shaded cube faces, cloud cubes distant, first-person right hand holding activeTool (pickaxe/axe/sword) with swing animation. No external assets. Prior view has palettes/art if useful. Stable eye height 1.6 tiles, no strong head bob or camera shake. Character body not drawn. Ray pick MUST select first visible intersected cube; solid obstacles block targeting through them. Labels upright legible Thai/English, combining marks dotted-circle, hide behind geometry, distance fade/cull to prevent clutter, completed labels show ครบแล้ว. Pickable letter plaques can resolve entity id only when not occluded; drops visible and ring when collecting. Hover outline/colored highlight active target. Near/far culling, perspective clip correct; bounded WebGL resources, dispose method deletes buffers/programs for retry. Use batching (one geometry buffer/draw pass) rather than hundreds individual drawcalls where practical.

API: constructor(canvas, overlayCanvas); resize() measures CSS rect, DPR cap2, sets this.width/height; render(game,dt,hoverId=null) copies current player camera then draws, does not mutate game; project(x,y,z=0) -> {x,y,visible,depth} where x,y are map coordinates and z is height in world tiles, CSS screen coords; pick(game) -> entity/drop id at center crosshair or null; dispose() cleans GPU buffers/program. Optional static pure projectPoint(worldPoint,camera,width,height), rayBox(origin,direction,min,max) for unit tests strongly encouraged. Camera: game.player={x,y,angle,pitch,swing}, angle yaw radians 0 faces +mapX, +pi/2 faces +mapY, pitch radians positive up, clamp +/-1.25 parent; height fixed1.6. Screen-right at yaw0 points +mapY. Parent core will move camera-relative; crosshair exactly width/2,height/2.

game state same as previous engine: size32, hearts5, invulnerable seconds, status ready/playing/won/lost, elapsed seconds, entities [{id,kind:tree|rock|animal,x,y,char,hp,maxHp,stun}], enemies same kind enemy, drops[{id,kind:letter|tool,char?,tool?,x,y}], tools{pickaxe,axe,sword:0..2}, activeTool, goals/inventory objects, collecting null or {dropId,elapsed:0..3}, selectedDrop()->nearest collectible drop. events array unique id kind hit/break/collect/hurt/tool; parent clears after render, read only.

Deliver polished working renderer, node --check, self-review. Report concerns. Save short report to docs/superpowers/lettercraft-first-person-renderer-report.md. Parent tests in real Chrome and integrates. No need to read whole spec; this supersedes isometric camera only. Plan UI footer remains tall~130px, top80px; crosshair screen center must remain unobscured. Put minimap top right below header. Physical attack range1.85 tiles; rock first spawn1 tile in front of player. Resource footprints radius.4; cube visuals should match approximately, rock height~1.0, tree trunk.4 wide height1.7 foliage height3, creature1.2-1.6.
