# วิธีย้าย NextGen Typing ขึ้น GitHub Pages

หน้าเว็บ (`index.html`) จะโฮสต์บน GitHub Pages ส่วนการเก็บข้อมูล (Google Sheets)
ยังใช้ Google Apps Script เหมือนเดิม โดยหน้าเว็บจะยิง `fetch` ไปหา Apps Script ให้เอง

---

## ขั้นที่ 1 — อัปเดต Google Apps Script (สำคัญ ต้องทำก่อน)

ไฟล์ `code.gs` ถูกแก้ให้รับคำสั่งบันทึกผ่าน POST แล้ว ต้องเอาขึ้น Apps Script ใหม่:

1. เปิดโปรเจกต์ Apps Script เดิม (script.google.com)
2. ก๊อปโค้ดจาก `code.gs` ในเครื่องนี้ ไปวางทับของเดิมทั้งหมด → บันทึก
3. กด **Deploy → Manage deployments**
4. กดรูปดินสอ (Edit) ที่ deployment เดิม → ช่อง **Version** เลือก **New version**
5. ตรวจให้แน่ใจว่า:
   - **Execute as**: Me
   - **Who has access**: **Anyone**  ← สำคัญมาก ไม่งั้น GitHub ยิงเข้าไม่ได้
6. กด **Deploy**

> ทำแบบ "Manage deployments → Edit → New version" จะได้ URL เดิม (`.../exec`)
> ตรงกับที่ตั้งไว้ใน `index.html` อยู่แล้ว ไม่ต้องแก้อะไรเพิ่ม
>
> ถ้าเผลอสร้าง deployment ใหม่จนได้ URL ใหม่ ให้เอา URL นั้นไปแก้ที่บรรทัด
> `const WEB_APP_URL = "..."` ใน `index.html`

---

## ขั้นที่ 2 — สร้าง GitHub Repo แล้วอัปโหลด index.html

ต้องอัปโหลดแค่ **`index.html`** ไฟล์เดียว (`code.gs` และ `DEPLOY.md` ไม่ต้องขึ้นก็ได้)

### วิธีง่ายสุด (ผ่านหน้าเว็บ GitHub ไม่ต้องใช้ command)
1. ไปที่ https://github.com/new
2. ตั้งชื่อ repo เช่น `nextgen-typing` → เลือก **Public** → กด **Create repository**
3. หน้าถัดไปกด **uploading an existing file**
4. ลากไฟล์ `index.html` เข้าไปวาง → กด **Commit changes**

---

## ขั้นที่ 3 — เปิด GitHub Pages

1. ใน repo ไปที่ **Settings → Pages**
2. หัวข้อ **Build and deployment → Source** เลือก **Deploy from a branch**
3. **Branch**: เลือก `main` โฟลเดอร์ `/ (root)` → **Save**
4. รอ 1–2 นาที รีเฟรชหน้า จะได้ลิงก์เว็บ เช่น
   `https://<username>.github.io/nextgen-typing/`

เปิดลิงก์นั้นได้เลย — เว็บจะโหลดข้อมูลจาก Google Sheets และบันทึกความคืบหน้ากลับได้ครบ

---

## ตรวจสอบว่าใช้ได้จริง

1. เปิดเว็บบน GitHub Pages
2. เพิ่มผู้ใช้ / เล่นผ่าน 1 ด่าน
3. เปิด Google Sheet ที่ผูกกับ Apps Script → ควรเห็นข้อมูลถูกเขียนเข้ามา
4. ถ้าบันทึกไม่เข้า: เปิด DevTools (กด F12) → แท็บ Console ดู error
   - เจอคำว่า CORS / 401 / 403 → กลับไปเช็คขั้นที่ 1 ข้อ 5 ว่า **Who has access = Anyone**

---

## หมายเหตุ

- โค้ดยังใช้กับ Apps Script ได้เหมือนเดิมทุกอย่าง (ตรวจ `google.script` ก่อน ค่อย fallback เป็น fetch)
  ดังนั้นเปิดผ่าน Apps Script URL เดิมก็ยังทำงานได้ปกติ
- Tailwind / Three.js / ฟอนต์ โหลดผ่าน CDN อยู่แล้ว จึงไม่มีไฟล์อื่นต้องอัปโหลด

---

## เกมท้ายบทเรียน: นักสำรวจโลกอักษร (Lettercraft)

เกมเดิม 4 ธีมถูกแทนที่ด้วยเกมโลกบล็อก 2.5D เกมเดียว เปิดจากปุ่มเกมหลังผ่านแบบฝึกหัดที่ความแม่นยำ 80% ขึ้นไป

### วิธีเล่น

1. คลิกเริ่มเพื่อล็อกเมาส์และมองผ่านสายตาตัวละครในโลกบล็อก 3D เลื่อนเมาส์หันซ้าย–ขวา มองขึ้น–ลง กด **Esc** เพื่อปล่อยเมาส์ แล้วคลิกในโลกเพื่อเล่นต่อ
2. ใช้ **W / ↑ เดินหน้า, S / ↓ ถอยหลัง, A D / ← → เดินด้านข้าง** ตามทิศที่มอง เข้าใกล้ต้นไม้ หิน สัตว์ หรือมอนสเตอร์ ใช้เป้ากลางจอเล็งแล้ว **คลิกซ้าย** เพื่อฟันหรือขุดจนตัวอักษรดรอป ไม่สามารถตีทะลุสิ่งกีดขวาง
3. หยุดใกล้ตัวอักษรที่ตกแล้ว **กดปุ่มตัวอักษรนั้นค้าง 3 วินาที** จนวงแหวนเต็มเพื่อเก็บ
4. เดินเก็บพลั่ว ขวาน และดาบ ระบบอัปเกรดและเลือกใช้ให้ ช่วยลดจำนวนครั้งและระยะห่างระหว่างการตี แต่ไม่ลดเวลาเก็บตัวอักษร
5. เก็บตัวอักษรจากบทเรียนให้ครบ **20 ชิ้นภายใน 5 นาที** มี **5 หัวใจ** โดนโจมตีสำเร็จครั้งละ 1 ดวง หลังโดนตีอมตะ 2 วินาที

หากตัวอักษรตรงกับปุ่ม WASD ให้หยุด ปล่อยปุ่ม แล้วกดใหม่เพื่อเก็บ ใช้ลูกศรเดินออกจากของได้ การเดิน ปล่อยปุ่ม กดผิด คลิกโจมตี หรือถูกโจมตีจะยกเลิกการเก็บ ขยับเมาส์เพื่อหันอย่างเดียวไม่ยกเลิก

เวลายังเดินเมื่อปล่อยเมาส์ เปิดวิธีเล่น หรือสลับแท็บ แต่การเดินและการต่อสู้จะหยุด เมื่อแพ้สามารถเล่นใหม่ได้โดยความคืบหน้าบทเรียนไม่หาย เกมนี้ไม่เพิ่มชีตหรือเปลี่ยน API ฐานข้อมูล และไม่บันทึกระดับบทเรียนซ้ำจากผลเกม

เกมใช้ WebGL และพื้นผิวพิกเซลที่สร้างขึ้นเองในโค้ด ไม่มีภาพหรือเสียงที่คัดลอกจาก Minecraft หากเบราว์เซอร์/หน้าต่างฝังไม่อนุญาต Pointer Lock เกมจะแจ้งให้คลิกเล่นและ **ลากเมาส์ขวาเพื่อมอง** แทน หากไม่มี WebGL จะแสดงวิธีกลับบทเรียนและคำแนะนำเปิดการเร่งกราฟิก

### แก้ไขซอร์สและสร้างไฟล์สำหรับ Deploy

ส่วนเกมแยกซอร์สไว้ให้ดูแลสะดวก:

- `src/lettercraft/core.js` — กติกาโลก อินพุต การโจมตี เก็บของ อุปกรณ์ ศัตรู และเวลา
- `src/lettercraft/view.js` — โลก WebGL มุมมองบุคคลที่หนึ่ง พื้นผิวพิกเซล กล้อง และการเล็งแบบรังสี 3D
- `src/lettercraft/bridge.js` — หน้าจอเกมและการเชื่อมบทเรียน
- `src/lettercraft/ui.html`, `style.css` — หน้าจอและการจัดวาง

หลังแก้ซอร์ส รันด้วย Node.js:

```powershell
node scripts/build-lettercraft.cjs
node scripts/build-lettercraft.cjs --check
node --test tests/block-world.test.cjs tests/lettercraft-view.test.cjs
```

ตัวสร้างจะรวมส่วนเกมลงใน `index.html` ดังนั้นไฟล์ Deploy ยังคงเป็น **`index.html` ไฟล์เดียว** ไม่ต้องอัปโหลดโฟลเดอร์ซอร์สสำหรับให้หน้าเว็บทำงาน อย่าแก้ส่วนเกมที่มีเครื่องหมาย `LETTERCRAFT_*` ภายในไฟล์รวมโดยตรง เพราะการสร้างครั้งถัดไปจะเขียนทับจากซอร์ส

ชุดทดสอบเบราว์เซอร์ `node tests/lettercraft-browser.cjs` ต้องมี Playwright และ Google Chrome พร้อมใช้งาน สามารถตั้ง `NODE_PATH` ชี้โฟลเดอร์แพ็กเกจที่ติดตั้งไว้แล้วได้ ชุดทดสอบใช้เซิร์ฟเวอร์ในเครื่องและดักคำขอ Apps Script จึงไม่เขียนข้อมูลลง Google Sheets จริง
